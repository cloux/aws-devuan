# *-*- Shell Script -*-*
# Print additional system info to the console
#
# (cloux@rote.ch)

#LOCAL_IP="$(hostname -I 2>/dev/null)"
#if [ "$LOCAL_IP" ]; then
#	msg " Local IP: $LOCAL_IP"
#	PUBLIC_IP="$(/usr/local/bin/public-ip 2>/dev/null)"
#	[ "$PUBLIC_IP" ] && msg "Public IP: $PUBLIC_IP"
#fi
